# This package contains some methods and algorithms related to power, modulas, add, subtract, multiplication and divison

__________

## 1) Power:
-   **Calculate num^pow**.In time O(log pow).[fastPower]
-   **Calculate (num^pow)%m**. [fastModularPower]